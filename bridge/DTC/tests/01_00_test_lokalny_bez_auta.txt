================================================================================
TEST 01 — Lokalny (bez auta), symulator          arkusz 01_00
================================================================================
Identyfikator: DTC-T01
Wersja arkusza: 00 (kolejne próby: skopiuj → 01_01_…, 01_02_…)
Powiązanie: DTC_REGRESSION_CHECKLIST.md §1

--------------------------------------------------------------------------------
CEL
--------------------------------------------------------------------------------
Potwierdzić, że pełny przepływ skanu DTC (bridge + Web UI) działa end-to-end
na symulatorze, bez pojazdu i bez interfejsu CAN.

--------------------------------------------------------------------------------
WYMAGANIA WSTĘPNE
--------------------------------------------------------------------------------
- Środowisko bridge uruchamialne (Python, zależności z projektu).
- Dostęp do Web UI zgodnie z dokumentacją bridge (port/adres jak u Ciebie w setupie).

--------------------------------------------------------------------------------
INSTRUKCJA WYKONANIA (krok po kroku)
--------------------------------------------------------------------------------
1. W katalogu projektu uruchom symulator:
      bridge/test_simulation.py
2. Uruchom Web UI (tak jak zwykle przy pracy z bridge).
3. W UI uruchom pełny skan DTC (przycisk „SKANUJ DTC” lub odpowiednik).
4. Obserwuj przebieg sesji do zakończenia (nie przerywaj wcześniej).

--------------------------------------------------------------------------------
OCZEKIWANE REZULTATY
--------------------------------------------------------------------------------
- Pojawia się panel / sekcja wyników skanu DTC.
- Status przebiegu przechodzi w kolejności: START → RUNNING → DONE.
- Dla każdego modułu w przebiegu skanu widać rekord typu module_result
  (lub równoważna prezentacja per moduł w UI).
- Po zakończeniu przycisk skanu wraca do stanu aktywnego (można ponowić skan).

--------------------------------------------------------------------------------
LOGI (osobne pliki — dołączasz na bieżąco)
--------------------------------------------------------------------------------
- Eksport DTC (UI / „zapisz logi”): bridge/DTC/logs/PQ35_DTC_DIAG_2026-04-12_10-43-20.txt

--------------------------------------------------------------------------------
OBSERWACJE (uzupełniamy podczas / po teście)
--------------------------------------------------------------------------------
Data wykonania:        2026-04-12
Wersja bridge/UI:      test_simulation.py + Web UI (lokalnie)

Przebieg statusów (START/RUNNING/DONE):
  Symulator uruchomiony; auto-scan uruchomiony; statusy uznane za prawidłowe (START → RUNNING → DONE).

Panel wyników — zachowanie / błędy UI:
  Wygląda w porządku; brak zgłoszonych problemów.

module_result per moduł — czy wszystkie widoczne:
  Tak — każdy moduł na liście.

Przycisk po DONE — stan:
  Poprawnie zablokowany na czas skanu i odblokowany po zakończeniu.

Odchylenia od oczekiwań / notatki:
  Brak.

Werdykt (PASS / FAIL / N/D):  PASS
